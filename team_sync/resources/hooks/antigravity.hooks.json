{
  "team-sync": {
    "enabled": true,
    "PreInvocation": [ { "hooks": [ { "type": "command", "command": "python3 -m team_sync hook antigravity pre_invocation", "timeout": 60 } ] } ],
    "Stop":          [ { "hooks": [ { "type": "command", "command": "python3 -m team_sync hook antigravity stop", "timeout": 120 } ] } ]
  }
}
